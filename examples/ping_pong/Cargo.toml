[package]
name = "ping-pong"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
moat-protocol = { path = "../../crates/moat-protocol" }
chrono = { version = "0.4", features = ["serde"] }
